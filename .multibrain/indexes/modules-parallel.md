# Sub-Index: `modules-parallel`

Migrasi 6 modul via sub-agent paralel (Services+Coverage, Customers+Broadcast, Finance, Settings+Manage+Account, Gateway+Template, Server OLT+Mikrotik).

## Rules

- Entry baru di ATAS.
- 1 baris per entry.

## Entries

- 2026-07-12 17:40 WIB | Droid | Retry isolir Mikrotik diperkuat tanpa ubah skema: `AutoController::isolir()` tetap retry semua order `Isolir` pada jadwal berikutnya, sekarang chunked + log warning router hilang/koneksi/command gagal + disconnect `finally` + summary output CronLog; pelanggan Active (sudah bayar) keluar dari antrean. Test mock gagal→retry sukses→Active skip PASS (13 assertion), Pint + full suite PASS. -> .multibrain/context/2026-07-12-1740-droid-retry-isolir-mikrotik.md
- 2026-07-10 WIB | claude-fable-5 | FITUR BARU: Laporan Gangguan (tiket dari chat WA). Tabel baru `gangguan_reports` (migration 2026_07_10_000003, additive: from_number/from_name/idpel/gateway/kategori/pesan/status[baru,diproses,selesai]/handled_by/catatan/resolved_at/timestamps) + model `GangguanReport` dgn `classify()` (keyword->kategori, WORD-BOUNDARY regex `(?<![\p{L}])kw(?![\p{L}])` supaya "lag" tak cocok "lagi"), `capture()` (dedup 12 jam, normalisasi 08->62, match Order by nomor, try/catch anti-gagalkan webhook), `kategoriLabel()`. Kategori: internet_mati/internet_lambat/tidak_bisa_akses/wifi/pembayaran/lainnya. HOOK penyerapan di DUA webhook: Meta `WebhookController::whatsappMeta` (setelah simpan inbox), gateway lama `whatsapp()` (awal, hanya chat individu non-grup non-fromMe). Admin: `Admin\GangguanController` (index: rekap bulanan per kategori + status breakdown + filter bulan/status/kategori + list paginate; updateStatus), route `routes/modules/gangguan.php` (`admin/gangguan`), view `admin/gangguan/index.blade.php` (kartu rekap + bar kategori "paling sering dilapor" + tabel + modal ubah status/catatan + tombol "Balas" via inbox HANYA utk gateway=meta -> reuse signature reply inbox yg sudah ada). Sidebar link "Laporan Gangguan" setelah Chat Whatsapp. Pagination pakai `pagination::bootstrap-5` (Laravel default Tailwind). Verifikasi: classifier 8/8 (termasuk tricky lagi/matikan), capture e2e (buat/dedup/normalisasi/abaikan-nonkeluhan/rekap) live DB, BROWSER test (login admin, /admin/gangguan render sempurna: rekap 3/1/1/1, bar kategori, tabel, Balas hanya di META, no console error), Pint+view:cache+route:list+suite PASS. BUG DITEMUKAN+DIPERBAIKI saat self-review: `GangguanController::updateStatus` redirect pakai `$request->only('bulan','status','kategori')` — 'status' dari form modal = status BARU laporan (baru/diproses/selesai), bocor jadi FILTER daftar (setelah tandai "selesai" list ter-filter cuma selesai). Fix: form kirim `f_status`/`f_kategori` (filter aktif) terpisah, redirect pakai itu (array_filter buang kosong). Terverifikasi tak bocor lagi. Belum di-commit.
- 2026-07-10 WIB | claude-fable-5 | Penyempurnaan menu existing (3): (1) Dashboard admin (`AdminController::index` + `admin/home.blade.php`) tambah 2 kartu: Total Tunggakan (Rp + jml, dari invoice status=Unpaid account=user SUM(price)/COUNT, link ke admin/finance/invoice) & Pelanggan Terisolir (Order status=Isolir COUNT, link ke admin/customers). Live: Rp218jt/990 tagihan, 483 isolir. (2) Rekap Absensi (`Admin\AttendanceController::report` + `admin/absensi/rekap.blade.php`) tambah tabel "Ringkasan per Karyawan" (collapse): groupBy user_id -> hitung hadir/izin/sakit/alpha/cuti/total + progress bar % kehadiran, sortByDesc total, kasus kosong ditangani. (3) Daftar pelanggan (`CustomerController::customerData`) tambah tombol WA per baris -> https://wa.me/{nomor} (nomor dinormalisasi 08->62), target _blank. Status badge & highlight expired sudah ada sebelumnya. Verifikasi: php -l, Pint, view:cache, query live semua OK, suite PASS. Belum di-commit.
- 2026-07-10 WIB | claude-fable-5 | Fitur baru: Radius Absen. Tabel baru `hr_attendance_setting` (migration 2026_07_10_000002, additive, single-row: label/latitude/longitude/radius_meter/enforce) + model `HrAttendanceSetting`. Admin: `Admin\AttendanceController::settings()/updateSettings()`, route `admin/absensi/pengaturan` (GET+POST), view `admin/absensi/settings.blade.php` dgn Leaflet map picker (klik peta -> isi lat/lng, lingkaran radius live saat input diubah) + toggle "enforce". Sidebar HRD/Karyawan tambah link "Pengaturan Radius Absen". Karyawan: `Karyawan\AttendanceController` tambah `distanceMeters()` (Haversine) + `checkRadius()` dipanggil di checkIn (hanya status hadir) & checkOut. Fail-open by design: kalau setting belum ada / enforce=false -> tidak diblok (instalasi lama aman). Kalau enforce=true: lat/lng jadi wajib (pesan jelas jika GPS mati), jarak >radius ditolak dgn pesan jarak+radius. View `karyawan/absensi.blade.php`: kotak `#gpsStatus` live feedback (mendeteksi/dalam radius/luar radius) + tombol submit (`.btn-absen-submit`) di-disable sampai lokasi valid — hanya aktif saat status="hadir" (izin/sakit/cuti tak perlu lokasi, tombol selalu aktif). Verifikasi: 8 skenario Feature test vs mysql (fail-open, dalam radius, luar radius+pesan, tanpa GPS, izin tanpa GPS, checkout luar/dalam radius, admin settings CRUD) 34 assertion PASS, test dihapus setelah lulus. Catatan: kolom `users.email` varchar(40) — email panjang senyap terpotong (strict mode off), sempat menyisakan 1 row test yg sudah dibersihkan manual. Pint clean, view:cache OK, route:list OK, suite utama PASS.
- 2026-07-10 WIB | claude-fable-5 | Konfirmasi pembayaran manual (`FinanceController::invoiceUpdate`, route `admin/finance/invoice/update` + finance-area `invoice/update`) kini BISA tanpa upload bukti. Form `admin/finance/invoice/edit.blade.php`: tambah radio `upload_bukti` (Ya=default / Tidak) + JS toggle sembunyikan/kosongkan file input & set `required` sesuai pilihan (section `@yield('scripts')`). Validasi: `image` jadi `required_if:upload_bukti,ya|nullable|...` (pesan pindah ke `image.required_if`). Controller: file-move dibungkus `if ($request->hasFile('image'))`, `$filename=''` bila tak upload → `bukti_pembayaran` kosong (laporan tampil "Tidak ada bukti", sudah ditangani `report/home.blade.php`). Verifikasi: temp Feature test vs mysql (actingAs admin) 2/2 PASS — "tidak"+tanpa file lolos validasi, "ya"+tanpa file tetap wajib bukti; test dihapus. Pint + view:cache + suite PASS.
- 2026-07-09 17:24 WIB | Droid | NMS Public Monitor 3 fix: (1) Logo tidak muncul -> path salah `asset($logo)` diperbaiki jadi `asset('assets/logo/'.$logo)` (kolom `website.logo` simpan nama file saja, konsisten dg layout OLT/router). (2) Refresh full-page dihapus: `location.reload()` diganti AJAX data-only `loadMonitorData()` (fetch map-data + status paralel Promise.all, render ulang marker/link/stats, marker stale dihapus, popup yang sedang terbuka dipertahankan via `getOpenDeviceId()`, guard `refreshInProgress`), tiap 60 detik hanya data yg diperbarui, viewport peta tidak reset. (3) Animasi fiberline di admin index sudah aktif (CSS `nmsLineFlow` + `.nms-flow-dash`/`.nms-flow-wireless` cocok dg class overlay di `addPolyline`). view:cache + Pint + PHPUnit PASS.
- 2026-07-09 17:13 WIB | Droid | NMS Public Monitor + admin map: fiber/copper link ikut jalan (OSRM) juga di `public.blade.php` (sebelumnya masih garis lurus di halaman public monitor yang dilihat user). Tambah `fiberStyle()/fetchRoadRoute()/drawFiberLink()` di public. Animasi aliran data: CSS `@keyframes nms-fiber-flow` + class `.nms-fiber-flow` (stroke-dashoffset), diterapkan ke garis fiber/copper di kedua map (admin index + public). Wireless tetap lurus tanpa animasi. view:cache + PHPUnit PASS.
- 2026-07-09 16:58 WIB | Droid | NMS Fiber Line mengikuti jalan: `resources/views/admin/nms/index.blade.php` update render link. Tambah `fiberStyle()`, `fetchRoadRoute()` via OSRM public `router.project-osrm.org` (GeoJSON full route), dan `drawFiberLink()`. Fiber/copper kini dirender mengikuti rute jalan, dengan sambungan dari titik device ke jalur jalan; wireless tetap garis putus-putus lurus. Jika OSRM gagal/tidak ada rute, fallback ke garis lurus agar link tetap terlihat. view:cache + PHPUnit PASS.
- 2026-07-09 16:49 WIB | Droid | NMS Fiber Line UX: backend/tabel `nms_links` dan modal link sudah ada tapi user minta tombol jelas di peta. Tambah tombol `Tarik Fiber Line` di header Peta Device NMS (`resources/views/admin/nms/index.blade.php`). Mode interaktif: klik tombol -> cursor crosshair -> klik device sumber -> klik device target -> validasi device berbeda & koordinat ada -> modal `Tambah Link Fiber` terbuka dengan Device A/B otomatis terisi, user tinggal isi port/type/status lalu Simpan. Tombol berubah jadi `Batal Tarik Fiber` saat mode aktif. route:list admin/nms, view:cache, PHPUnit PASS.
- 2026-07-09 16:38 WIB | Droid | Audit + hardening fitur Absensi & panduan README. (1) Timezone: `now()` app = UTC, absensi kini pakai `nowWib()` (Asia/Jakarta) di `Karyawan\AttendanceController` (tanggal & jam check-in/out) + `Admin\AttendanceController::reportFilters()` default WIB. (2) Flow izin/sakit/cuti tidak lagi dianggap check-in (check_in/GPS/foto hanya utk status hadir); view `karyawan/absensi.blade.php` tampilkan info status non-hadir & sembunyikan tombol check-out; dropdown check-in hilangkan `alpha`. (3) Validasi kini terlihat: semua `validate()` di kedua controller di-catch `ValidationException` -> flash `auth_errors` (view hanya render auth_errors). (4) CSV injection: `exportCsv` neutralize sel diawali `= + - @` via `csvSafe()`. (5) Foto: ekstensi dari `$file->extension()` (bukan client), nama pakai WIB. (6) `CheckLevel` putus sesi technician yg dinonaktifkan admin (status_account Non Active) di request berikutnya. (7) `updateEntry` tambah `check_out after_or_equal:check_in`. README: tambah section "Folder Upload Absensi Karyawan" (`public/data/absensi`, chmod/mkdir, storage:link opsional). php -l, route:list, view:cache, Pint (2 fixed), PHPUnit PASS.
- 2026-07-09 16:03 WIB | Droid | Fitur baru Absensi + Login Karyawan (role `technician`, TANPA ubah skema). Tabel baru `hr_attendances` (migration `2026_07_09_120001`, additive, pola nms_*/acc_*) + model `HrAttendance`. Login karyawan dibuka: `AuthController::auth()`/`redirectByLevel()` + `CheckLevel` tambah `technician` -> `karyawan/absensi`. Admin: `Admin\AttendanceController` (data karyawan CRUD tipis buat/reset-pass/aktif-nonaktif, rekap absensi filter tanggal/karyawan/status, export CSV streamed, edit manual entry) route `routes/modules/attendance.php` (`admin/karyawan`, `admin/absensi/rekap`), view `admin/absensi/{karyawan,rekap}.blade.php`. Karyawan self-service: `Karyawan\AttendanceController` (check-in/out + GPS opsional + foto selfie ke `public/data/absensi`, cegah dobel, riwayat) route `routes/modules/karyawan.php`, view `karyawan/{absensi,history}.blade.php`. Form Manage User tambah opsi level Technician. Sidebar: section "HRD / Karyawan" utk admin, sidebar diringkas (hanya Absensi+Riwayat) + sembunyikan link Pengaturan utk role technician. migrate --force OK, route:list, view:cache, Pint, PHPUnit PASS.
- 2026-07-09 00:03 WIB | Droid | Fix grafik Traffic Monitor tidak tampil: script Highcharts+AJAX dipindah dari `@section('content')` ke `@section('scripts')` (layout load jQuery setelah content), container `#trafficMonitor` diberi tinggi eksplisit 330px, cek `typeof Highcharts === 'undefined'`, tampilkan alert error jika AJAX gagal. route `router/traffic/fix`, view:cache, Pint, PHPUnit PASS.
- 2026-07-08 23:55 WIB | Droid | Router dashboard Traffic Monitor UI: tampilkan dropdown interface dari data `/interface/print`, fallback ke interface DB jika list kosong, AJAX traffic diganti ke POST `router/traffic/fix` dengan CSRF dan interface terpilih, chart title ikut berubah saat interface dipilih, data chart di-reset saat switch interface, request pertama langsung jalan. Simplify tooltip/format bits. view:cache, Pint, PHPUnit PASS.
- 2026-07-08 23:11 WIB | Droid | Fix RouterOS 6/7 dashboard 500 `Undefined array key 0` di `app/Http/Controllers/Server/RouterController.php`: tambah helper `rosRows()`, `rosFirstRow()`, `trafficResponse()`, dashboard kini aman saat `/system/routerboard/print`, `/system/resource/print`, `/system/clock/print`, log, interface return kosong/trap/non-array; default model/timezone/architecture/version/uptime aman; traffic AJAX tidak akses `[0]` langsung. View `resources/views/server/router/dashboard.blade.php`: log key pakai fallback dan hapus CDN jQuery ganda. php -l, route:list dashboard, view:cache, Pint, PHPUnit PASS.
- 2026-07-08 21:38 WIB | Droid | Audit & hardening tombol Generate Invoice + Update Status: (1) Generate: validasi bulan/tahun/idpel, cek order&service eksis, cek duplikat + insert dalam DB::transaction lockForUpdate, invoice dibuat DULU sebelum WA/email, WA & Brevo dibungkus try/catch (gagal hanya log), kode invoice unik dicek di transaksi, false-success pada pelanggan kosong dihilangkan, tombol disable-on-submit. (2) Update Status: validasi server-side (target/code/idpel/status=Paid/category/metode/image max 2MB), nilai price/package/expdata RE-DERIVED dari DB (hidden field tidak dipercaya), invoice & order & report dalam DB::transaction, cek invoice sudah Paid ditolak, cek router eksisten, WA terbayar dibungkus try/catch, filename pakai Str::random. php -l, view:cache, Pint, PHPUnit PASS.
- 2026-07-08 21:21 WIB | Droid | Fix data Laporan Tagihan tidak muncul: opsi Status default value="" tidak cocok dengan backend yang cek `=== 'Tampilkan Semua'` -> query `where('status','')` = 0 baris. Perbaikan: controller `getFilteredData()` dinormalisasi (empty = Tampilkan Semua / Pilih Paket), logika filter disederhanakan, order DESC. View: opsi Paket/Status kembalikan value sentinel, hapus CDN jQuery/exceljs eksternal (layout sudah punya jQuery lokal -> filter cepat), tambah loading row + error row di AJAX. Tinker: DEFAULT_COUNT=255, PAID=175, UNPAID=80 untuk bulan berjalan. view:cache, Pint, PHPUnit PASS.
- 2026-07-08 21:07 WIB | Droid | Redesign UI Laporan Tagihan (`admin/finance/report`): nav tombol lebih jelas, filter card rapi, table hover/align-middle, export di header, link bukti pembayaran diganti tombol biru `Lihat Bukti`, status auto payment jadi badge, empty state colspan benar. view:cache, Pint, PHPUnit PASS.
- 2026-07-08 20:44 WIB | Droid | Redesign UI Generate Invoice: card lebih lebar dan centered, field bulan/tahun/pelanggan pakai form-select-lg, hint keamanan, tombol kembali, tahun tidak hardcoded lagi (range tahun berjalan sampai 2020, default bulan/tahun berjalan). php -l, view:cache, Pint, PHPUnit PASS.
- 2026-07-08 19:48 WIB | Droid | Fix logic pembayaran invoice manual: counter Paid/Unpaid casing benar; invoice/order/report diupdate dulu setelah open isolir, WA terbayar setelah DB sukses, email Brevo gagal hanya log warning dan pembayaran tetap sukses; path Isolir PPPoE/Hotspot kini insert Report pemasukan. php -l, route:list 273, view:cache, Pint, PHPUnit PASS -> .multibrain/context/2026-07-08-1948-droid-invoice-payment-logic-fix.md
- 2026-07-08 19:31 WIB | Droid | Audit production menu/route/controller: fix dobel prefix route finance-area (`finance/finance/kas` dst), fix return URL Tripay, netralisir form PPP disabled, lengkapi sidebar Coverage (ODC/Area/Customer/RXPower) + Server (ACS/Voucher). route:list 273, view:cache OK, Pint+PHPUnit PASS. Audit 78 url Blade -> 6 false positive dinamis/file, tidak ada link rusak -> .multibrain/context/2026-07-08-1931-droid-production-audit.md
- 2026-07-08 12:45 WIB | claude-fable-5 | Tambah 2 template Meta baru `notif_isolir` (var: nama, id_pelanggan, jatuh_tempo, link) & `notif_buka_isolir` (var: nama, id_pelanggan, paket, link) di `WhatsAppMetaApi::defaultTemplateDefinitions()`. Tambah event `EVENT_ISOLIR`/`EVENT_BUKA_ISOLIR` + map `DEFAULT_TEMPLATE_NAMES` di WhatsAppNotifier (templateName direfactor pakai map). Halaman meta-templates tampilkan 6 template + mapping isolir/buka-isolir. CATATAN: AutoController::isolir() legacy TIDAK kirim WA — pengiriman notif isolir belum di-wire ke cron (nunggu keputusan user); template + resolver sudah siap. Pint + AdminSmokeTest PASS.
- 2026-07-08 12:30 WIB | claude-fable-5 | Fix Meta Phone Number ID 16 digit (`1233925339801612`) tidak muat `whatsapp_setting.sender` varchar(15): tanpa ubah skema, Phone Number ID disimpan di compact Meta blob `api_url` field `phone_number_id` indeks 8, kolom sender Meta diisi penanda pendek `meta`; semua send path pakai `WhatsAppGatewayResolver::sender()`. Form add/edit mirror input Phone Number ID ke hidden `meta_phone_number_id`. Pint + AdminSmokeTest PASS.
- 2026-07-08 12:00 WIB | claude-fable-5 | Audit halaman `admin/whatsapp/meta/templates`: semua elemen page diverifikasi fungsional (dropdown gateway auto-submit, form cek template, tabel template lokal+remote, create 4 template default + konfirmasi Swal, form kirim test, tombol Edit Gateway). Fix minor: field `language` di form create sekarang benar2 dipakai (sebelumnya diabaikan — selalu 'id'), footer template ambil dari definisi, validate() diganti redirect+auth_errors (konsisten pattern app). Tidak ada elemen mati yang perlu dihapus.
- 2026-07-08 11:30 WIB | claude-fable-5 | Persiapan deploy production: SEMUA 29 migration diberi guard `Schema::hasTable()` → `php artisan migrate` di DB production existing aman (skip tabel yang sudah ada, hanya buat yang baru: cache, jobs, wa_inbox_messages). Prosedur deploy: backup dulu (`mysqldump`), lalu `migrate:status` untuk preview, baru `migrate`. JANGAN PERNAH `migrate:fresh`/`migrate:refresh` di production — itu DROP semua tabel.
- 2026-07-08 11:00 WIB | claude-fable-5 | Smoke test 87 route GET aman (login developer, exclude route destruktif) via `tests/Feature/AdminSmokeTest.php` — awalnya 5 route 500 karena tabel `bot_olt`/`logs_voucher`/`orders_voucher`/`template_message_voucher` TIDAK ADA di DB landaknet (CI4 pun pasti error di halaman itu). Fix: guard `Schema::hasTable` di VoucherController & OltController::botWhatsapp → halaman render kosong + warning, bukan 500. Smoke test PASS 87/87; auto-skip kalau DB bukan mysql. Jalankan: `DB_CONNECTION=mysql DB_DATABASE=landaknet php artisan test --filter=AdminSmokeTest`.
- 2026-07-08 10:30 WIB | claude-fable-5 | Audit logic WA Meta Official — 3 bug diperbaiki: (1) template `notif_tagihan` cuma 4 param padahal semua pemanggil EVENT_TAGIHAN kirim 5 (nama hilang & Meta reject param mismatch) → body template jadi 5 param; SIAPA YANG SUDAH TERLANJUR CREATE TEMPLATE DI META harus create ulang via menu Meta Templates. (2) `sendMessageMedia` kirim `caption` untuk audio/sticker → Graph API reject; caption kini di-skip untuk tipe itu. (3) Encode meta settings ke `api_url` varchar(255) bisa terpotong diam-diam → validasi panjang (juga sender>15) di whatsappAdd/whatsappUpdateNumber. Plus: webhook Meta sekarang dedup `meta_message_id` (Meta retry delivery = pesan dobel di inbox). Pint+test PASS.
- 2026-07-08 03:00 WIB | claude-9router | Inbox WhatsApp jadi realtime via polling AJAX: endpoint `GET admin/whatsapp/inbox/poll` (messages + conversations + can_reply_text), view auto-refresh tiap 5 detik, append pesan baru, refresh daftar percakapan, reload saat status 24h window berubah. Pint/PHPUnit PASS.
- 2026-07-08 02:30 WIB | claude-9router | Tambah modul WhatsApp Inbox (omnichannel sederhana native): tabel baru `wa_inbox_messages`, webhook Meta simpan pesan masuk + auto-reply, controller `Admin\WhatsAppInboxController` (index/send), route `admin/whatsapp/inbox`, view inbox.blade.php (list percakapan + thread + form balas + cek 24h CS window), menu sidebar "Inbox Chat". Tidak ubah skema legacy. Migrate + route:list + Pint/PHPUnit PASS -> .multibrain/context/2026-07-08-0230-claude-9router-wa-inbox.md
- 2026-07-08 01:50 WIB | claude-9router | Ubah kategori default template WhatsApp Meta dari `MARKETING` ke `UTILITY` di `app/Libraries/WhatsAppMetaApi.php`; tampilan Meta Templates dan create-template kini pakai kategori Utility. Pint/PHPUnit PASS.
- 2026-07-08 01:40 WIB | codebuddy | WA Meta Official dipindah ke menu WhatsApp Gateway: Graph URL, WABA ID, verify token, language, dan nama template tagihan/reminder/terbayar/pelanggan_baru disimpan dari form admin di row gateway aktif tanpa ubah skema DB; notifier tidak lagi bergantung env untuk template Meta. Test PASS -> .multibrain/context/2026-07-08-0140-codebuddy-wa-meta-menu-settings.md
- 2026-07-08 01:25 WIB | codebuddy | Fix template notifikasi WA + switch gateway: `WhatsAppNotifier` memastikan tagihan/pengingat/terbayar/pelanggan_baru pakai Meta `sendTemplate()` saat Meta aktif dan `sendMessage()` saat gateway lama aktif; lookup gateway aktif sekarang `mode=on`; tambah mapping env template Meta. Pint/test PASS -> .multibrain/context/2026-07-08-0125-codebuddy-wa-template-switch-fix.md
- 2026-07-08 01:10 WIB | codebuddy | Integrasi WhatsApp Meta Official: library `WhatsAppMetaApi`, `WhatsAppGatewayResolver` (switch gateway lama vs Meta tanpa ubah skema), webhook verify+receiver `/webhook/whatsapp/meta`, admin gateway setting provider dropdown, template manager + test send. 258→262 routes, Pint 8 fixed, test PASS -> .multibrain/context/2026-07-08-0110-codebuddy-wa-meta-official.md
- 2026-07-08 00:45 WIB | codebuddy | Audit bug setelah sinkronisasi legacy: perbaiki route order `customers.php` (wildcard tertangkap sebelum endpoint khusus), fallback `Schema::hasTable('website')` di `AuthController::websiteData()` untuk test env. Pint fixed 35 style issues. Test PASS 2/2 -> .multibrain/context/2026-07-08-0045-codebuddy-bug-audit.md
- 2026-07-08 00:30 WIB | codebuddy | Port sinkronisasi legacy terbaru: modul P0 (Callback, Webhook, Auto), modul P1 (ACS, Voucher), endpoint P2 (customer export/filter/tikor, coverage area/customer/rxpower, invoice print/delete/bulk, admin webhook, getPPPOEUsers, get-odp-by-area, router traffic fix, services get_profile). Total routes 207→258, syntax OK -> .multibrain/context/2026-07-08-0030-codebuddy-modules-p0-acs-voucher.md
- 2026-07-07 22:25 WIB | claude-fable-5 | Finance admin selesai (34 routes: 27 admin/finance/* + 7 AJAX global level admin,developer,finance). Quirk dipertahankan: notifikasi WA/email dikirim SEBELUM update DB (gagal email = invoice batal, persis CI4); export excel ke public/laporan; view kas CI4 0-byte → blade kosong; pay hanya tripay (paydisini = area user).
- 2026-07-07 23:12 WIB | factory-worker | Server ACS & Voucher selesai (18 routes). Library ACSRequest di-port apa adanya. Logic 1:1, view pakai layout admin. -> .multibrain/context/2026-07-07-2312-factory-worker-acs-voucher.md
- 2026-07-07 23:15 WIB | codebuddy | Modul P1-P3 selesai: Finance area (`/finance/*` level:finance), User area (`/user/*`), Guest cek/tagihan (`/cek`,`/tagihan`), AJAX (`/ajax/cekcoupon`), router/traffic endpoints. 207 routes, 0 error. P0 (Callback/Webhook/Auto) ditunda butuh port 1:1 penuh + verifikasi router live -> .multibrain/context/2026-07-07-2315-codebuddy-modules-p3-p1.md
- 2026-07-07 14:45 WIB | claude-fable-5 | Server OLT+Mikrotik selesai (36 routes). Catatan: tabel `bot_olt` & `services_voucher` tak ada model → `DB::table()` langsung. `olt/settings` method+view CI4 kosong → dipertahankan. `router/dashboard` panggil AJAX `router/traffic` (belum dimigrasi, grafik traffic kosong sampai modul itu di-port). Typo `dashbard` OLT dipertahankan sesuai CI4.
- 2026-07-07 14:30 WIB | claude-fable-5 | Customers+Broadcast selesai (15 routes). Catatan: `admin/export` di CI4 = dead route (method tidak ada) → dibuat export Excel PhpSpreadsheet. `sendbroadcastmedia` CI4 kosong → di-port kirim media via WhatsAppApi. Route `add/proses` didaftar SEBELUM `add/{any}` biar tidak ketangkep wildcard.
- 2026-07-07 14:20 WIB | claude-fable-5 | Gateway+Template selesai (30 routes: payment/whatsapp/email gateway + template message).
- 2026-07-07 14:18 WIB | claude-fable-5 | Settings+Manage+Account selesai (16 routes).
- 2026-07-07 14:15 WIB | claude-fable-5 | Services+Coverage selesai (11 routes: service ISP + odp/olt/pool coverage).
- 2026-07-07 14:10 WIB | claude-fable-5 | 6 sub-agent paralel di-spawn. 3 sisa (Customers+Broadcast, Finance, Server) resumed setelah 429 rate limit — belum selesai.
