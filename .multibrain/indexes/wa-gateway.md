# wa-gateway — WhatsApp Gateway (lama + Meta Official) & template

Work log, entry 1 baris, terbaru di ATAS.

- 2026-07-10 WIB | claude-fable-5 | notif_isolir_v2 tombol jadi DINAMIS (`.../tagihan/{{1}}`) — sebelumnya statis. Data: dari 66 order Active overdue cuma 40 punya invoice Unpaid (26 tidak) & 76/1297 order tanpa invoice sama sekali → kalau suffix = kode invoice bisa null → Meta tolak. Solusi: suffix = `$unpaidInvoice?->code ?? $order->idpel` (idpel SELALU ada) di AutoController isolir; `$link` & body ikut `url('tagihan/'.$payRef)`. GuestController::tagihanDetail dibuat pintar: kalau {code} bukan invoice, coba sbg idpel -> redirect ke tagihan Unpaid terbaru; kalau tak ada -> redirect ke `tagihan` (cek page) BUKAN `invoice` (yg 404). Verifikasi temp Feature test (guest, mysql): kode->200, idpel->redirect ke kodenya, ngawur->redirect cek; + Http::fake: isolir_v2 create url `.../tagihan/{{1}}`, send ada komponen button suffix. Pint+suite PASS.
- 2026-07-10 WIB | claude-fable-5 | UI meta-templates.blade.php: tampilkan URL tombol Bayar + tombol "Salin URL" per template bertombol. Ada input `#btnDomain` (default APP_URL) yang bisa diisi domain produksi → JS `refreshBtnUrls()` update semua `.btn-url` = `{domain}/{path}{suffix}`. Suffix `/{{1}}` (dinamis) / '' (statis) dirender via `@php $btnSuffix` (di dalam @php `{{1}}` aman dari parser Blade) lalu ditaruh di `data-suffix` supaya JS tak perlu tulis `{{` literal. Tujuan: user salin URL utk field URL tombol saat daftar template manual di Meta, dan atasi masalah base URL ke-patri localhost (tinggal isi domain). Bukti Blade::render: dinamis=`.../tagihan/{{1}}`, statis=`.../tagihan`. view:cache OK.
- 2026-07-10 WIB | claude-fable-5 | UI meta-templates.blade.php: tombol "Salin Isi Pesan" (`.btn-copy-message`) menyalin PESAN LENGKAP = header+body+footer (join \n\n) TANPA label tampilan "Header:"/"Footer:" — FOOTER IKUT (user revisi: awalnya minta tanpa footer, lalu minta footer ikut). Plus ikon salin Header & Footer terpisah (`.btn-copy-el` data-copy-target). Teks komponen dipisah ke span `.tpl-header-text`/`.tpl-footer-text` & `<pre>.tpl-body-text` (label "Header:"/"Footer:" ada di `<strong>` terpisah shg tak ikut). Handler baca `textContent`.trim() via `closest('td')`. Reuse `copyToClipboard()`. Bukti: hasil copy notif_tagihan_terbayar_v2 = header+body+footer, footer ada, tanpa label. view:cache OK.
- 2026-07-10 WIB | claude-fable-5 | KONFIRMASI: variabel `link` template Meta LAMA TIDAK di-rename (permintaan user), tapi NILAI-nya kini link pembayaran — dibuktikan via Http::fake capture payload: notif_tagihan {{4}} & notif_pengingat {{3}} & notif_isolir {{4}} = url('tagihan/{code}'), notif_tagihan_terbayar {{3}} = url('invoice/{code}'); jumlah param terkirim pas (6/4/4/5, slicing bekerja). Efek LANGSUNG tanpa re-approval Meta (yang berubah nilai runtime, bukan definisi template). Komentar stale di defaultTemplateDefinitions (urutan arg pemanggil lama) diperbarui.
- 2026-07-10 WIB | claude-fable-5 | Rename label variabel Meta v2 + link ganda. Format tetap ANGKA `{{n}}` (bukan NAMED), tapi label `parameters` diganti: `link`->`link_bayar`/`link_invoice`/`link_web` sesuai peran → badge preview & copy-var tampil nama jelas. tagihan_v2 (jadi 7 var: +`link_invoice` {{7}}) & pengingat_v2 (jadi 5 var: +`link_invoice` {{5}}) kini punya DUA link (bayar+invoice); terbayar_v2 link->link_invoice; isolir_v2 link->link_bayar; daftar/buka_isolir link->link_web. Pemanggil tagihan (Auto+Finance) & pengingat (Auto) kirim `url('invoice/{code}')` sbg param tambahan di URUTAN TERAKHIR. KRITIS: `WhatsAppNotifier::sendNotification` tambah `templateParamCount()` + slice `array_slice($params,0,$expected)` → template LAMA (6 var) tetap terima 6 walau pemanggil kirim 7, jadi tidak rusak selama transisi. Preview meta-templates.blade.php diperbaiki: kini tampilkan header/footer/badge Tombol (sebelumnya cuma body → user kira belum diupdate). Verifikasi: 12/12 struktur valid ({{n}} seq, var=body=example), simulasi slice old=6/v2=7 benar, Pint+view:cache+suite PASS. (A) Variabel free-text baru `{link_bayar}` (=url tagihan/{code}) & `{link_invoice}` (=url invoice/{code}) ditambah di semua pengisi $message (AutoController tagihan/pengingat, FinanceController tagihan/terbayar, CallbackController terbayar); `{link_web}` lama TETAP. Hint variabel ditambah di view Template Message. Cukup tulis `{link_bayar}` tanpa `/tagihan/{nomor_invoice}` lagi. (B) Tombol URL "Bayar Sekarang" di template Meta v2: `WhatsAppMetaApi::createTemplate` dukung komponen BUTTONS (dinamis `.../tagihan/{{1}}` + example, atau statis `.../tagihan`), `sendTemplate` kirim komponen `button sub_type=url index=0` bila ada suffix. `WhatsAppNotifier::sendNotification` tambah arg `$buttonSuffix`, hanya dikirim bila template punya tombol DINAMIS (`templateHasDynamicButton`) → template lama tanpa tombol tak terpengaruh. tagihan_v2/pengingat_v2 = tombol DINAMIS (suffix = kode invoice, selalu ada), isolir_v2 = tombol STATIS ke halaman cek tagihan (isolir belum tentu punya 1 invoice). Pemanggil tagihan/pengingat/isolir kirim suffix. Verifikasi Http::fake: payload create (dinamis vs statis) & send (with/without button) benar; kirim isolir tanpa suffix tetap valid; var free-text resolve. Pint+view:cache+suite PASS.
- 2026-07-10 WIB | claude-fable-5 | Template Meta versi lengkap `_v2` (6 buah: notif_tagihan_v2, notif_pengingat_v2, notif_tagihan_terbayar_v2, notif_daftar_berhasil_v2, notif_isolir_v2, notif_buka_isolir_v2) ditambah di `WhatsAppMetaApi::defaultTemplateDefinitions()` meniru teks "Template Message" lama (salam + kalimat lengkap). NAMA BARU sengaja: template lama tetap APPROVED & notif tetap terkirim selama v2 menunggu verifikasi Meta (user minta "verifikasi Meta tdk pengaruhi pengiriman"). Urutan {{n}} v2 = identik versi lama → pemanggil sendNotification() tak diubah. Ditambah dukungan `example` di `createTemplate()` (Meta wajibkan contoh utk body bervariabel) + `whatsappMetaCreateTemplates` idempoten (skip template yg sudah ada di Meta, pesan sukses/skip). LINK dirapikan jadi link PEMBAYARAN langsung (bukan beranda): tagihan/pengingat -> `url('tagihan/{code}')`, terbayar -> `url('invoice/{code}')` di AutoController/FinanceController/CallbackController (hanya param Meta, teks $message lama tak disentuh). ISOLIR: `$link` (dipakai teks + param Meta) diubah dari `url('/')` ke `url('tagihan/{invoice_unpaid}')` (cari Invoice Unpaid per idpel, fallback `url('tagihan')`) — sebelumnya teks bilang "silakan bayar via link" tapi mengarah ke beranda. terbayar/buka_isolir/pelanggan_baru tetap portal (bukan skenario bayar). Cara pakai: menu Meta Templates → Create (push v2) → tunggu APPROVED → ganti nama di form gateway ke `_v2` (semua 6 field sudah ada + hint ditambah). Verifikasi: 12/12 definisi valid ({{n}} sekuensial, param=caller argc=example), Pint + view:cache + suite PASS.
- 2026-07-10 WIB | claude-fable-5 | SECURITY: webhook receiver Meta (`/webhook/whatsapp/meta`) kini validasi `X-Hub-Signature-256` (HMAC-SHA256 raw body + App Secret) via `verifyMetaSignature()`. Aktif hanya bila `META_APP_SECRET` diisi (config `services.whatsapp_meta.app_secret`), fail-open bila kosong agar instalasi lama tetap terima pesan. Sebelumnya payload webhook Meta diterima tanpa autentikasi (bisa dipalsukan → inject inbox palsu / picu balasan WA keluar). Test sig salah=403 & sig benar=lolos PASS. Set `META_APP_SECRET` di `.env` untuk mengaktifkan proteksi.
- 2026-07-08 20:41 WIB | Droid | Sidebar admin: `Chat Whatsapp` dipisah jadi menu utama sendiri (`admin/whatsapp/inbox`), tidak lagi di dalam submenu `Whatsapp Gateway`; submenu gateway tetap Setting, Test Message, Meta Templates. view:cache, Pint, PHPUnit PASS.
- 2026-07-08 20:37 WIB | Droid | Inbox reply signature sekarang bisa dipilih: auto pakai nama admin login atau manual input per pesan. Controller validasi `signature_mode` auto/manual dan `signature_name` wajib saat manual, lalu append `\n\n~{signature}` sebelum kirim Meta. php -l, Pint, PHPUnit, view:cache PASS.
- 2026-07-08 20:14 WIB | Droid | Tambah signature admin otomatis di setiap pesan balasan inbox: pesan keluar otomatis di-append `\n\n~{nama admin}` sebelum dikirim via Meta API dan disimpan ke `wa_inbox_messages` bersama signature. Cek panjang 4000 char setelah signature. Pint, PHPUnit, view:cache PASS.
- 2026-07-08 20:08 WIB | Droid | Audit WA Inbox Meta end-to-end (webhook->store->poll->send). Fix: send reply cek error Graph API dan simpan status gagal sebagai alert (sebelumnya selalu dianggap sukses). Inbox view: tambah banner petunjuk setup kalau Meta gateway OFF atau webhook OFF, tampilkan webhook URL. php -l, route:list, view:cache, Pint, PHPUnit PASS. -> .multibrain/context/2026-07-08-2008-droid-wa-inbox-meta-audit.md
- 2026-07-08 16:35 WIB | kiro | Rebrand semua template WA ke "ANNORTY NET"; `notif_pelanggan_baru` (ditolak Meta krn keyword kredensial) → password dibuang, body netral, di-rename jadi `notif_daftar_berhasil` (6 param). Sesi Meta template **DI-PENDING** atas permintaan user; sisa kerja di sisi Meta Dashboard + fitur edit-template-from-app belum dibuat. -> .multibrain/context/2026-07-08-1635-kiro-wa-meta-template-rename.md
- 2026-07-08 02:30 WIB | claude | 9-router support + WA Inbox send (Meta reply). -> .multibrain/context/2026-07-08-0230-claude-9router-wa-inbox.md
- 2026-07-08 01:40 WIB | codebuddy | Semua setting Meta pindah ke menu WhatsApp Gateway (tanpa .env). -> .multibrain/context/2026-07-08-0140-codebuddy-wa-meta-menu-settings.md
- 2026-07-08 01:25 WIB | codebuddy | Fix switch template lama vs Meta (sendTemplate/sendMessage). -> .multibrain/context/2026-07-08-0125-codebuddy-wa-template-switch-fix.md
- 2026-07-08 01:10 WIB | codebuddy | Port WhatsApp Meta Official (library, resolver, webhook, template manager). -> .multibrain/context/2026-07-08-0110-codebuddy-wa-meta-official.md
- 2026-07-08 00:20 WIB | factory-droid | Audit template WA. -> .multibrain/context/2026-07-08-0020-factory-droid-wa-template-audit.md
